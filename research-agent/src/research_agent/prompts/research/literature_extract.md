Extract only from the supplied verified metadata and acquired content. Preserve the source layer and provenance IDs for every statement. Never invent a DOI, year, benchmark result, section, or page. Keep limitations_claimed separate from limitations_inferred, and label every inferred limitation as agent inference. Abstract-only content permits only high-level problem, method claim, and stated-result extraction. If required content is missing, return a partial extraction or SEARCH_REQUEST instead of filling from model memory.
